# 项目进度日志

说明：本文件按时间保留历史决策。2026-09-11 的 motif-first 内容用于还原当时进展；自 2026-09-13 起，以 DNA 大模型嵌入为当前有效主线，传统方法只保留小型解释基线。

## 2026-09-05：项目初始化

- 解析课程与 PR01-02 任务边界，明确只做启动子调控模式发现；
- 固化 RQ1-RQ3、数据字段、目录职责和可复现要求；
- 接入并锁定 \`baselines/memesuite-lite\` 子模块；
- 建立 README、问题建模、数据调研、实验计划和报告目录。

## 2026-09-11：M1/M2 数据管线与早期 motif 方案

- 实现启动子 metadata schema、DNA 标准化、质量审计、精确去重和来源级 split；
- 实现 N0 单核苷酸背景、长度/GC/分组/位置 EDA、质量报告和 manifest；
- 实现 B0 \`-35/-10 box\` 共识 PWM 的双链扫描；
- 当时按 motif-first 思路规划 MEME/FIMO；该思路已由 2026-09-13 主线调整取代，B0 继续保留为解释基线；
- demo 输入为 8 条，清洗后 6 条；这些数据不产生生物学结论。

## 2026-09-11：方法学基础修正

- 新增 N1 二核苷酸保持打乱，和 N0 共同作为背景对照；
- 新增 TSS 窗口与链方向标准化，负链做反向互补；
- 新增近同源聚类，使 discovery/validation 不被近同源序列跨 split 泄漏；
- 新增 B0 按序列长度校准阈值，避免把过低固定阈值当成显著性；
- 新增对应的端到端 demo、单元测试和运行说明；
- 这些工作服务于后续大模型 embedding 的数据质量和独立验证，不改变主线判断。

## 2026-09-13：DNA 大模型嵌入主线落地

### 主线决定

- 将项目统一为“DNA 预训练模型表示 → 聚类/近邻 → 局部遮挡解释 → 跨来源验证”；
- 锁定 \`InstaDeepAI/nucleotide-transformer-v2-50m-multi-species\`；
- 固定 revision \`f1ff02bacb08d292ee9c3e227356d692055ac50d\`、mean pooling、随机种子和设备记录；
- DNABERT-2 只保留为模型不可用时的备选，不并行铺开两套大模型；
- MEME/DREME/STREME + FIMO 不作为大模型主线，但保留一套覆盖全序列、sigma 分组、FIMO 和间距统计的课程必做线。

### 已实现

- 新增 \`models/dna_embedding.py\`：真实 Hugging Face 后端和显式受限的 \`hash-smoke\` 测试后端；
- 新增 \`models/embedding_analysis.py\`：PCA、KMeans、silhouette、近邻、ARI/NMI 诊断和遮挡重要度；
- 新增 \`experiments.run_embedding\`：统一输出向量、索引、聚类、近邻、遮挡、摘要和 manifest；
- 针对本机 Transformers 5.8 与固定模型旧代码的接口差异增加内存兼容层，不修改模型缓存；
- 更新 README、问题建模、数据调研、运行说明、实验计划和模板；
- 保留远程更新的 N0/N1、TSS 窗口、近同源聚类和 B0 校准实现。

### 实际证据

- 真实模型在 6 条 demo 上生成 \`6×512\` embedding；
- 对 2 条 demo 序列生成 8 个遮挡窗口；
- 固定配置重复运行的 embedding SHA256 为 \`c1079af371e58b7057c5c028a738f649b02851bc9d7deb9018c560fd14c760a8\`；
- embedding、模型版本、输入、配置和输出哈希均写入 manifest；
- 合并远程更新后，全套自动化测试为 63 个，全部通过；
- demo 结果只证明工程链路，不能解释为新的 motif 或 sigma 生物学规律。

## 当前状态

已完成：问题建模、数据质量基础设施、真实模型推理、L0-L2 分析入口、同源泄漏控制、测试/追踪框架，以及 RegulonDB/DBTBS 真实来源下载、字段适配和第一轮数据构建。

待完成：数据再分发条件的进一步确认、统一 TSS 窗口确定、真实数据 embedding、跨物种/跨来源复现，以及老师要求的一套全序列/分组 MEME/DREME/STREME + FIMO motif/间距实验。

准确表述是：**真实数据已经下载并完成第一轮质量构建；真实数据 embedding、正式模式实验和最终生物学结论尚未完成。**

## 2026-09-13：P1 数据来源审计第一小步

- 完成 RegulonDB 和 DBTBS 官方页面、版本线索、内容范围和许可边界的初步核验；
- 将审计状态写入 `configs/data_sources.example.yaml`，不填未经确认的下载 URL、文件名和 SHA256；
- 新增 `docs/数据来源审计_20260913.md`，记录已确认信息、版本冲突和未决项；
- 当前结论：RegulonDB v12.0.0 可作为 E. coli 核心来源候选，DBTBS 可作为 B. subtilis 跨物种候选，但两者都还不能写成“正式数据已接入”；
- 本步没有下载数据、没有运行正式实验，也没有修改 M1 会议纪要。

## 2026-09-16：P3 字段适配第一版

- 新增 `preprocessing/source_adapters.py`，支持常见来源列名到统一 metadata schema 的显式映射；
- 对必填字段缺失直接报错，对 sigma、TSS、链方向、证据等可选字段不做推断；
- 保留 `source_record_id`，并为无法确认的字段生成适配报告，便于后续 Gate A 复核；
- 新增适配器单元测试，覆盖别名映射、显式物种、进入现有清洗管线、缺字段和冲突列；
- 更新运行说明，真实文件下载后可先适配再进入清洗和 embedding；
- 全套自动化测试增至 68 个并全部通过；
- 本步仍未下载外部原始数据，也未运行真实数据 embedding。

## 2026-09-16：按老师要求落地真实来源并完成第一轮构建

- 根据老师原文 PR01-02 的数据要求，实际下载 RegulonDB 官方 `PromoterSet.gff3`、E. coli K-12 genome FASTA，以及 DBTBS ver4 release 4.1 的 promoter index 和 1,062 个详情页；
- RegulonDB 解析 4,042 条记录，DBTBS 解析 698 条有效 sigma-promoter 记录；两来源合并 4,740 条；
- 完成真实数据字段适配、全局 ID 检查、来源级 discovery/validation 划分和输入哈希记录；
- 第一轮来源原生窗口清洗后保留 4,489 条，删除空序列 92 条、重复序列 159 条，非法字符与高 N 含量均为 0；
- 近同源聚类得到 4,481 个簇，跨 discovery/validation 的同源簇为 0，报告的泄漏违规数为 0；
- 由于 DBTBS 缺少可确认的统一 TSS/链方向，本轮没有伪造坐标，也没有把来源原生窗口当作统一 TSS 实验；
- 新增 `download_regulondb.py`、DBTBS 抓取器、来源合并器和 `configs/m1_real_native_20260916.yaml`，并将 URL、访问日期、SHA256、过滤计数和当前边界写入[数据下载记录](数据下载记录_20260916.md)；
- 本步仍未运行真实数据 embedding，下一步先完成窗口 Gate A，再进行真实数据 L0 小批量 pilot。

## 2026-09-21：目录整理与当前状态复核

- 本地 `main` 已快进同步 GitHub 的 README 更新；同时修复 README 末尾乱码，没有改变代码或实验结果；
- 将老师原始任务 PDF 放入仓库 `docs/` 作为本地阅读材料，并把文档中的外部路径改为仓库内路径；
- 清理父目录中的旧版 `preprocessing/`、`tests/`、重复会议纪要，以及仓库内 `tmp/`、测试缓存和 `__pycache__/`；真实数据目录保留；
- 当前准确状态仍是：真实来源下载和第一轮质量构建已完成，M2 的 `-10/-35 box` 位置分析、真实数据 embedding、正式 motif/FIMO/间距实验和跨来源验证仍待完成。

## 2026-09-22：Gate A 条件复核与真实数据 L0/L1 推进

- 对 `data/processed/m1_real_native_20260916/promoters_clean.tsv` 完成 Gate A 复核：RegulonDB 清洗后 3,813 条均有本地 TSS/链方向并可支持 `[-60,+20]`；DBTBS 清洗后 676 条的 TSS/链方向仍全部缺失，因此统一 TSS 的跨来源正式实验未通过；复核记录见 `docs/GateA复核_20260922.md`；
- 将来源原生窗口 pilot 限定为资源和工程验证：每个来源固定抽取 32 条，共 64 条，CPU 运行约 19.06 秒，输出 `64×512`；两次运行 embedding SHA256 均为 `63082a4c40a42676fdc545c088a217dc9e07d591167774ccdee0c408106fb791`，生成 14 个遮挡窗口；
- 在相同固定 revision、pooling、批大小和随机种子下完成全部 4,489 条来源原生窗口的 L0/L1 运行，CPU 约 92.57 秒，输出 `4489×512`，embedding SHA256 为 `99ce9c46505013faec8d22d58d8ac73c2efb3b598901eb07da61c818d009a1c3`；
- 完成第一版偏差诊断：3 个聚类的 silhouette 为 `0.04036`；source 的 ARI/NMI 为 `0.00322/0.00214`，sigma 的 ARI/NMI 为 `0.00045/0.00523`；PCA 第 2 轴与 GC 的 Spearman 相关为 `0.2125`；近邻同源/同物种率为 `0.7184`，已知 sigma 对近邻的同组率为 `0.2676`；这些是描述性诊断，不构成调控模式结论；
- 新增 `experiments/diagnose_embedding.py` 及测试，诊断产物位于被忽略的 `results/l1_structure_real_native_20260922/`；完整运行配置为 `configs/l0_embedding_real_native_20260922.yaml`；
- `preflight` 显示 PyTorch/Transformers/Accelerate 可用，但本机没有可执行的 MEME/STREME/DREME/FIMO/TOMTOM 工具；传统课程 motif 线仍待环境解决，不能用 B0 PWM 扫描代替；运行中 Transformers 5.8 的 Hub safetensors 自动转换线程出现 `KeyError: event_id`，但固定缓存权重推理和产物生成成功，作为环境警告保留；
- 使用 RegulonDB 已确认 TSS/链方向的来源内子集运行 `configs/m1_regulondb_tss_20260922.yaml`：4,042 条输入经 TSS 窗口保留 3,950 条，去重后得到 3,807 条统一 81 bp 序列；在该子集上完成 N1 B0 校准与扫描，保留 557 个精确共识命中（-10：367，-35：190），正向命中相对 TSS 起点中位数为 -13 bp 和 -33 bp；新增 `experiments/summarize_b0_positions.py` 生成位置表和图；但 81 bp 背景 FPR 为 0.13265，高于预注册 0.05，故只作为初步位置描述；
- 当前边界更新为：真实数据 embedding 已有一轮可追溯的来源原生结果，但统一坐标、局部遮挡正式分析、跨来源复现和课程 motif/FIMO/间距实验仍未完成。
